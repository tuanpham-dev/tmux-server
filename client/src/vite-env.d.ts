/// <reference types="vite/client" />

// Injected by vite.config.ts `define` — shown in the Settings footer as
// ground truth for which build a device is actually running.
declare const __BUILD_TIME__: string;
