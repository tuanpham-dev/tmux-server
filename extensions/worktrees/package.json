{
  "name": "worktrees",
  "private": true,
  "publisher": "tmux-server",
  "version": "1.0.0",
  "displayName": "Worktrees",
  "description": "Palette entry points for the PROJECTS tree's create-worktree form. Worktrees themselves are core: they are the tree's middle level (project → worktree → terminal), with git listing, creation and removal owned by the server (bundled — see extensions/build.mjs).",
  "icon": "./icon.svg",
  "tmuxServer": {
    "client": "./dist/client.js"
  }
}
