{
  "name": "ghostty-engine",
  "private": true,
  "publisher": "tmux-server",
  "version": "1.0.0",
  "displayName": "Ghostty Terminal Engine",
  "description": "The ghostty-web (WASM) terminal rendering engine — the default desktop engine. Disabling it falls back to the required xterm.js engine (bundled — see extensions/build.mjs).",
  "icon": "./icon.svg",
  "dependencies": {
    "ghostty-web": "0.4.0"
  },
  "tmuxServer": {
    "client": "./dist/client.js"
  }
}
