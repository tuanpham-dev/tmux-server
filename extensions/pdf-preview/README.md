# PDF Preview

A PDF viewer tab, opened instead of trying to render `.pdf` files as text or downloading them.

## Contributes

- **File viewer:** `.pdf` files open as a rendered, scrollable, zoomable preview tab.

## Notes

Bundled with tmux-server — disable it from the Extensions view to fall back to downloading PDF files instead of previewing them.
