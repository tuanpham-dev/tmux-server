# Image Preview

An image viewer tab with zoom and pan, opened instead of trying to render an image file as text.

## Contributes

- **File viewer:** common raster/vector image formats open as a zoomable, pannable preview tab instead of a text editor.

## Notes

Bundled with tmux-server — disable it from the Extensions view to fall back to downloading image files instead of previewing them.
