{
  "name": "live-preview",
  "private": true,
  "publisher": "tmux-server",
  "version": "1.0.0",
  "displayName": "Live Preview",
  "description": "Built-in live-reloading HTML preview (bundled — see extensions/build.mjs).",
  "icon": "./icon.svg",
  "contributes": {
    "configuration": {
      "title": "Live Preview",
      "properties": {
        "livePreview.autoRefresh": {
          "type": "boolean",
          "default": true,
          "description": "Automatically reload the preview when the HTML file or a sibling file in its folder changes."
        },
        "livePreview.pollInterval": {
          "type": "integer",
          "default": 1000,
          "minimum": 250,
          "maximum": 10000,
          "description": "How often (ms) to check the previewed folder for changes when auto-refresh is on."
        }
      }
    }
  },
  "tmuxServer": {
    "client": "./dist/client.js",
    "server": "./server.js"
  }
}
